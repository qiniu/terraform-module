---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "qiniu_compute_instance Resource - qiniu"
subcategory: ""
description: |-
  用于创建和管理Qiniu LAS实例的 resource
---

# qiniu_compute_instance (Resource)

用于创建和管理Qiniu LAS实例的 resource



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `image_id` (String) 镜像ID
- `instance_type` (String) 实例类型，指定实例的规格
- `system_disk_size` (Number) 系统盘大小，单位是GiB

### Optional

- `cost_charge_type` (String) 实例计费类型
- `description` (String) 实例描述
- `hostname` (String) 主机名称，指定实例的主机名
- `internet_max_bandwidth` (Number) 公网最大带宽，单位是Mbps，取值范围0-200，默认值为0，表示不分配公网IP
- `name` (String) 实例名称，指定实例的名称
- `password` (String, Sensitive) 实例密码，如果未指定则随机生成。（TODO: 当前修改密码会造成重装系统，后续如有修改密码API，则可能会优化）
- `placement_group_id` (String) 置放组ID，指定实例所属的置放组，如果不指定，则不加入任何置放组
- `state` (String) 实例状态
- `system_disk_type` (String) 系统盘类型，指定系统盘的存储类型
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `user_data` (String) 用户数据，指定实例的初始化脚本或配置

### Read-Only

- `cpu` (Number) 主机核心数量
- `created_at` (String) 实例创建时间，格式为RFC3339
- `id` (String) 实例ID
- `image_name` (String) 镜像名称
- `memory` (Number) 主机内存大小，单位是GiB
- `private_ip_addresses` (Attributes List) (see [below for nested schema](#nestedatt--private_ip_addresses))
- `public_ip_addresses` (Attributes List) (see [below for nested schema](#nestedatt--public_ip_addresses))
- `region_id` (String) 实例所在的区域ID
- `region_name` (String) 区域名称
- `system_disk_id` (String) 系统盘ID

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) 一个可被解析为持续时间的字符串（参考 time.ParseDuration 文档），由数字和单位后缀组成，例如 "30s" 或 "2h45m"。有效的时间单位包括："s"（秒） "m"（分钟） "h"（小时）
- `delete` (String) 一个可被解析为持续时间的字符串（参考 time.ParseDuration 文档），由数字和单位后缀组成，例如 "30s" 或 "2h45m"。有效的时间单位包括："s"（秒） "m"（分钟） "h"（小时）。在销毁操作发生之前，将超时更改保存到状态中时，删除操作才适用。
- `read` (String) 一个可被解析为持续时间的字符串（参考 time.ParseDuration 文档），由数字和单位后缀组成，例如 "30s" 或 "2h45m"。有效的时间单位包括："s"（秒） "m"（分钟） "h"（小时）。读取操作发生在任何刷新或计划操作期间，当启用刷新时。
- `update` (String) 一个可被解析为持续时间的字符串（参考 time.ParseDuration 文档），由数字和单位后缀组成，例如 "30s" 或 "2h45m"。有效的时间单位包括："s"（秒） "m"（分钟） "h"（小时）


<a id="nestedatt--private_ip_addresses"></a>
### Nested Schema for `private_ip_addresses`

Read-Only:

- `ipv4` (String) 内网IPv4地址


<a id="nestedatt--public_ip_addresses"></a>
### Nested Schema for `public_ip_addresses`

Read-Only:

- `bandwidth` (Number) 公网IP带宽，单位是Mbps
- `id` (String) 公网IP地址ID
- `ipv4` (String) 公网IPv4地址
- `traffic` (Number) 公网IP流量，单位是GB
