- version: "0.1.0"
  description: "这是一个示例应用版本配置文件，包含了应用版本的基本信息和输入参数的定义。"
  status: "Published" # 默认是Draft，如果需要发布改为Published
  deployMeta:
    # inputSchemaInline: {}
    inputSchemaRef: "../common/inputSchema.yaml"
    # inputPresetsInline: []
    inputPresetsRef: "./inputPresets.yaml"
    terraformModule:
      gitSource: 
        repo: "https://github.com/qiniu/terraform-module"
        ref: "main"
        directory: "openclaw"
      # moduleContent: |
      #   output "example_output" {
      #     value = "This is an example output from the OpenClaw Terraform module."
      #   }